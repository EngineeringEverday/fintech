# SettleIQ — runtime dependencies
# Python 3.10+ required (uses zoneinfo, match statements)
streamlit>=1.30

# All other imports are Python stdlib only.
# Optional LLM providers (activate with USE_REAL_LLM=true in .env):
# openai>=1.0
# anthropic>=0.20
